# Mouse mode!
set -g mouse on

# True Color!
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color*:Tc"

# No status bar!
set -g status off

# Use base16 colors!
source-file ~/.colors/tmux.conf
